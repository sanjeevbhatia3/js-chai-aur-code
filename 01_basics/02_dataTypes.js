"use strict"; // "use strict" is a directive that helps to catch common coding errors and "unsafe" actions such as defining global variables
// It helps to write cleaner and more secure JavaScript code
// treat all JS code as new ECMAScript 5 code

// alert("Hello World"); we are using node.js, not browser 

let firstName = "John"; // string
let phoneNumber = 1234567890; // number
let isActive = true; // boolean
let accountBalance; // undefined
let accountCity = null; // null

console.log(typeof firstName); // string

//symbol => unique and immutable data type (use in react)

// typeof operator
// typeof operator returns the type of the variable
// typeof null is an object
// typeof null is a bug in JavaScript

// object -> it is type of null
// type of undefined is undefined